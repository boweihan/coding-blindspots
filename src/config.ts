//This should be the backend URL so you can proxy the request to it
const backendurl = 'http://localhost:8000' 
const frontendurl = 'http://localhost:8080' 

const baseUrl = frontendurl;

export default { baseUrl };
